let listitemTemplate = document.getElementById("listitem");

const savesActivities = JSON.parse(localStorage.getItem('activities')) || [];

function{
    console.log(listitemTemplate.dataset.id); 
}

// save to localstorage, when choos, put that one into session ->
    //go new page, load session storage
    // ?!